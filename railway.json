{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "cd backend && python -m flask run --host=0.0.0.0 --port=$PORT"
  }
}
