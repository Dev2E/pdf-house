{
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/converter"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
